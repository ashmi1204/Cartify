spring.application.name=backend
# Database Connection Properties
spring.datasource.url=jdbc:mysql://localhost:3306/project-database?createDatabaseIfNotExist=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=Ashmi@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate Configuration (Recommended)
# update: Hibernate updates the schema based on your entities. Use 'none' for production.
spring.jpa.hibernate.ddl-auto=update
# Shows the SQL statements generated by Hibernate in the console
spring.jpa.show-sql=true
# Specifies the SQL dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
